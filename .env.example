# Base URL of the backend API, including the version prefix.
# Must match FRONTEND_URL in the backend's .env for CORS/cookies to work
# (backend default: http://localhost:5173, this dev server's default port).
VITE_API_URL=http://localhost:3000/v1
