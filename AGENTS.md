# تعليمات العمل في المشروع

## نطاق العمل

- افهم بنية المشروع وتعليماته قبل إجراء التعديلات.
- التزم بالمهمة المطلوبة، ولا تبدأ مراجعة شاملة أو إعادة هيكلة
  خارج نطاقها إلا بطلب المستخدم.
- إذا طلب المستخدم شرحًا أو مراجعة فقط، لا تعدّل الملفات.
- حافظ على السلوك الحالي وعقود API، باستثناء التغييرات المطلوبة صراحةً.
- أعد استخدام الأدوات والأنماط الموجودة، وتجنب التعقيد والمكتبات غير الضرورية.

## TypeScript والتحقق من البيانات

- لا تستخدم any أو as any أو @ts-ignore أو تحويلات غير آمنة
  لإخفاء مشكلات الأنواع.
- حافظ على إعدادات TypeScript الصارمة.
- تعامل مع البيانات الخارجية غير المتحقق منها كـ unknown.
- لا تعتبر generics أو type assertions تحققًا أثناء التشغيل.
- افحص البيانات قبل قراءة حقولها أو تخزينها أو استخدامها في منطق العمل.
- استخدم أدوات التحقق الموجودة في المشروع، مثل type guards أو Zod.
- طابق الفحوص مع عقد البيانات الفعلي، بما يشمل:
  الحقول المطلوبة والاختيارية، وnull وundefined،
  وعناصر المصفوفات والكائنات المتداخلة وأشكال الاستجابة المختلفة.
- لا تخمّن حقولًا أو تفرض قيودًا غير موجودة في العقد.
- اجمع الأنواع والفحوص المشتركة في مكان مناسب.
- تحقّق عند حدود دخول البيانات، وتجنب إعادة فحص بيانات سبق التحقق منها.
- لا تضف فحوصًا لجسم استجابة لا يستخدمه التطبيق.
- عند طلب مراجعة شاملة، ابدأ بالمصادقة والمستخدم والصلاحيات،
  ثم بقية البيانات التي يستهلكها التطبيق.

## معالجة الأخطاء

- تعامل مع الأخطاء كـ unknown، وافحص نوعها قبل قراءة خصائصها.
- افحص جسم استجابة الخطأ قبل استخدام محتواه.
- تعامل بأمان مع أخطاء الشبكة وغياب الاستجابة.
- حافظ على سلوك المصادقة وإعادة المحاولة وحالات التحميل.
- نظّف الحالة غير المكتملة عند الفشل دون مسح الحالة الصحيحة عند النجاح.

## الاختبارات والتحقق

- عند تعديل التحقق من البيانات، أضف أو حدّث اختبارات مفيدة
  لقبول البيانات الصحيحة ورفض البيانات غير المتوقعة.
- غطِّ الحالات المؤثرة، مثل القيم الناقصة والأنواع الخاطئة
  والبيانات المتداخلة وأشكال الاستجابة المتعارضة.
- استخدم أدوات الاختبار الموجودة في المشروع.
- شغّل فحص TypeScript والاختبارات المناسبة للتغييرات.
- أصلح المشكلات الناتجة عن تعديلاتك دون إضعاف الفحوص.
- نجاح TypeScript لا يثبت صحة البيانات أثناء التشغيل.

## التواصل والنتيجة

- إذا كان عقد البيانات غير واضح، ابحث عن مصدره واسأل عند الضرورة.
- اشرح بالعربية ما تغيّر وسبب التغيير.
- اذكر الملفات المعدّلة ونتائج الفحوص والاختبارات.
- وضّح ما لم تختبره أو ما بقي غير مؤكد.
- لا تدّعِ أن المشروع بالكامل خالٍ من الأخطاء.